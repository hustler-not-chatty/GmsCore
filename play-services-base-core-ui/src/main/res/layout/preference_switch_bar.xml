<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ SPDX-FileCopyrightText: 2020, microG Project Team
  ~ SPDX-License-Identifier: Apache-2.0
  -->

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:hz="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="checked"
            type="boolean" />

        <variable
            name="enabled"
            type="boolean" />

        <variable
            name="description"
            type="String" />

        <variable
            name="callback"
            type="org.microg.gms.ui.PreferenceSwitchBarCallback" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{(v) -> callback.onChecked(!Boolean.valueOf(checked))}"
        android:clickable="@{enabled}"
        android:focusable="@{enabled}"
        app:cardCornerRadius="24dp"
        android:layout_margin="16dp"
        app:strokeColor="@android:color/transparent"
        hz:cardBackgroundColorAttr="@{checked ? androidx.appcompat.R.attr.colorControlActivated : (enabled ? androidx.appcompat.R.attr.colorButtonNormal : androidx.appcompat.R.attr.colorControlHighlight)}"
        android:clipChildren="false"
        android:clipToPadding="true"
        app:strokeWidth="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:paddingStart="?attr/listPreferredItemPaddingStart"
            android:paddingLeft="?attr/listPreferredItemPaddingLeft"
            android:paddingEnd="?attr/listPreferredItemPaddingEnd"
            android:paddingRight="?attr/listPreferredItemPaddingRight"
            tools:background="?attr/colorControlActivated">

            <TextView
                android:id="@android:id/title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="2"
                android:text="@{description}"
                android:layout_marginVertical="24dp"
                style="@style/SnowUI.SettingsBase.MainSwitchText"
                hz:textColorAttr="@{checked? com.google.android.material.R.attr.colorOnPrimary: com.google.android.material.R.attr.colorOnSurface}"
                tools:text="Enabled" />

            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switch_widget"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:background="@null"
                android:checked="@{checked}"
                android:enabled="@{enabled}"
                app:onCheckedChangeListener="@{(v, newStatus) -> callback.onChecked(newStatus)}"
                tools:checked="true" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</layout>
